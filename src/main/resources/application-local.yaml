spring:
  docker:
    compose:
      file: docker/compose.yaml
  config:
    import: "optional:file:./docker/.env[.properties]"
  flyway:
    locations: classpath:db/migration,classpath:db/seed-local
logging:
  level:
    com.galileo.ecommerce: debug
app:
  security:
    jwt:
      secret: ${JWT_SECRET}
